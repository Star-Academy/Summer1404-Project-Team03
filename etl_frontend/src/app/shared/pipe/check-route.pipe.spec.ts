import { CheckRoutePipe } from './check-route.pipe';

describe('CheckRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
